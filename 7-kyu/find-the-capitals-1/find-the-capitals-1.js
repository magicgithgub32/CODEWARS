 
var capitals = function (word) {
  
  let arr = word.split("")
  
  let arrSol = []
  
  let capWord = word.toUpperCase()
  
  let arrCap = capWord.split("")
  
  for(letter of arr) {
    if (letter === )
  }
  
  
  
};