function toFreud(string) {
let result = []
​
let arr = string.split(" ")
​
if (string === "") {
  return string
}
​
for (let i = 0; i < arr.length; i++) {
  result.push('sex')
}
  return result.join(" ")
  
}